name: mobile_fe
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  cupertino_icons: ^1.0.8
  image_picker: ^1.2.2
  url_launcher: ^6.3.2
  cached_network_image: ^3.3.0
  dio: ^5.9.2
  firebase_core: ^4.9.0
  firebase_messaging: ^16.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # Mendaftarkan folder assets supaya logo bisa terbaca oleh aplikasi
  assets:
    - assets/images/